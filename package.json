{
  "name": "world-geojson-tools",
  "version": "0.0.0",
  "private": true,
  "description": "World GeoJSON monorepo: the JavaScript client and its adapters (packages/js/*) and the pinned mapshaper the data pipeline runs. The root is not published.",
  "type": "module",
  "scripts": {
    "previews": "wgj previews",
    "validate": "wgj validate",
    "build:client": "npm run build -w packages/js/geoworld",
    "build": "npm run build:client && npm run build --workspaces",
    "lint": "npm run build:client && npm run lint --workspaces",
    "test": "npm run build:client && npm test --workspaces"
  },
  "devDependencies": {
    "mapshaper": "0.7.63"
  },
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "packages/js/*"
  ]
}
