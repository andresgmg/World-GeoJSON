# World GeoJSON — documentation toolchain.
#
#   python -m venv .venv
#   .\.venv\Scripts\python.exe -m pip install -r requirements-docs.txt
#   .\.venv\Scripts\python.exe -m mkdocs serve
#
# Pinned for reproducible builds; Dependabot opens PRs for bumps.
# Do NOT pin pymdown-extensions / pygments here — mkdocs-material owns them
# and pinning separately means fighting the resolver.

mkdocs==1.6.1
mkdocs-material==9.7.7

# Catalog navigation. The catalog pages themselves are written by
# scripts/gen_catalog.py (a MkDocs hook); literate-nav turns the SUMMARY.md
# it emits into the Catalog nav subtree.
#
# Note: mkdocs-gen-files is deliberately NOT used. It writes virtual pages
# outside docs_dir, and mkdocs-static-i18n drops those silently.
mkdocs-literate-nav==0.6.3

# Bilingual site (English default, Spanish).
mkdocs-static-i18n==1.3.1

# "Last updated" stamps. Requires fetch-depth: 0 in CI.
mkdocs-git-revision-date-localized-plugin==1.6.0

# Streaming JSON scanner used by scripts/build_manifest.py. Not needed to
# build the docs; kept here so a docs-only environment can still regenerate a
# manifest. The lint/test toolchain lives in requirements-dev.txt.
ijson==3.5.1
