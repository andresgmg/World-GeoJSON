# World GeoJSON — lint, type-check and test toolchain for scripts/ and tests/.
#
#   pip install -r requirements-dev.txt
#   ruff check . && ruff format --check . && mypy && pytest
#
# Pinned for reproducible CI; Dependabot opens PRs for bumps. The docs
# toolchain lives in requirements-docs.txt and is not needed here.

ruff==0.16.8
mypy==2.3.1
pytest==9.1.1

# The pipeline itself, as an editable install: puts `wgj` on the path and
# pulls its runtime dependencies (ijson for streaming scans, jsonschema for
# the contract). Pinned here so CI is reproducible.
-e ./pipeline[pipeline]
ijson==3.5.1
jsonschema==4.26.0
